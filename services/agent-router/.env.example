# Agent Router Configuration

# Server port
PORT=8787

# Authentication token (generate a secure random string)
TOKEN=your-secret-token-here

# Enable simulator mode (creates fake agents for testing)
SIMULATOR_MODE=false
